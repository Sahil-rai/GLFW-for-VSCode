{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: Build Engine",
            "command": "C:/Mingw_Kit/w64devkit/bin/g++.exe",
            "args": [
                // 1. Debug flags
                "-g",
                "-std=c++17",
                "-mconsole",

                // 2. Header include paths (-I)
                "-I${workspaceFolder}/include",
                "-I${workspaceFolder}/include/glad",
                "-I${workspaceFolder}/buffers", // <-- PATH TO NEW .H FILES
                "-I${workspaceFolder}/C:\\libs\\glm",

                // 3. Library paths (-L)
                "-L${workspaceFolder}/lib",

                // 4. Source files (ALL .cpp and .c files)
                "${workspaceFolder}/src/main.cpp",
                "${workspaceFolder}/src/glad.c",
                "${workspaceFolder}/src/shaderClass.cpp", // <--- ADDED!
                "${workspaceFolder}/buffers/EBO.cpp",
                "${workspaceFolder}/buffers/VAO.cpp",
                "${workspaceFolder}/buffers/VBO.cpp",
                "${workspaceFolder}/src/stb_image.cpp",

                // 5. Linking (System libraries)
                "-lglfw3",
                "-lopengl32",
                "-lgdi32",
                "-luser32",
                "-lshell32",
                "-lws2_32",
                "-lversion",

                // 6. Output executable
                "-o",
                "${workspaceFolder}/cutable.exe"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build engine with W64DevKit GCC"
        }
    ]
}
